name: running_historian
description: "Беговой гид для бегунов"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_map: ^8.2.2
  latlong2: ^0.9.1
  geolocator: ^11.1.0
  shared_preferences: ^2.2.2

  just_audio: ^0.9.46
  flutter_tts: ^4.2.3

  path_provider: ^2.0.15
  path: ^1.8.2

  hive: ^2.2.3
  hive_flutter: ^1.1.0

  flutter_background_service: ^5.0.5
  flutter_background_service_android: ^6.0.3

  permission_handler: ^11.0.1  # 👈 Добавьте эту строку
  app_settings: ^5.1.1  # 👈 Добавьте эту строку

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true  # 👈 Исправьте на "true"
  assets:
    - assets/music.mp3